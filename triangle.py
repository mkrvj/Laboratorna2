def area(a, h):
    """
    функция area возвращает площадь треугольника
    """
    return a * h / 2
def perimeter(a, b, c):
    """
    функция perimetr возвращает периметр треугольника
    """
    return a + b + c
